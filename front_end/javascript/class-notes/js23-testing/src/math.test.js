// ! node module export

const {
  calculateTotal,
  celsiusToFahrenheit,
  fahrenheitToCelsius,
  fibonacci,
} = require("./math.js");

// ! es6 module format

// import { calculateTotal, celsiusToFahrenheit, fahrenheitToCelsius, fibonacci} from "./math";
